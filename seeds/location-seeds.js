//const sequelize = require('../config/connection');
const { location } = require('../models')

//id, Restaurant number, restaurant addresss 

const locationdata =[



]

const seedLocation = () => Comment.bulkCreate(locationdata);

module.exports = seedLocation;
